# Grimoire

A companion web app for running and playing Shadowdark RPG — for players, for the GM, and for AI-run tables. This is attempt two; attempt one ("Delve", repo `Yetiouz/yetidark`) is archived on its `attempt-1` branch with DB dumps — reference for lessons, never a source to copy from.

## Current state

- `index.html` — the landing page (single self-contained file, dark theme, inline styles/assets).
- `/app` — the Vite + React + TypeScript + Tailwind v4 scaffold (Milestone 1 foundation). The design system exists: a closed set of 10 named typography levels (`app/src/lib/typography.ts` — display/h1/h2/h3/body/bodySecondary/caption/label/numeric/dataDisplay, sized via `--text-*` theme tokens in `index.css`), a closed spacing scale (4/8/12/16/24/32/48/64px, six named slots — documented live in the style guide's Spacing section), a closed icon system (lucide-react, always through `app/src/components/ui/Icon.tsx` — fixed 24px grid/stroke weight, three color states, a closed `name` set — documented live in the Iconography section), a UI kit (`app/src/components/ui/` — Panel, Button, Badge, StatusChip, StatTile, LogEntryRow, TorchTimer, DiceResult, PortraitAvatar, SceneDivider, DangerBanner, EmptyState, Skeleton/SkeletonGroup, ErrorBanner, TextInput, Modal, Icon), and a living style-guide page (`app/src/pages/style-guide/`) still reachable at the `/style-guide` pathname (a bare `window.location.pathname` check in `App.tsx` — still no router, per SPEC's Journal v1 plan: "plain state is fine for v1," deep-linkable URLs are a noted fast-follow). Four font families: Instrument Sans (`--font-sans`, body copy and other UI text), Bebas Neue (`--font-heading`, h1/h2/h3 headlines — condensed, uppercase, single weight), Chivo Mono (`--font-mono`, labels/numerics/captions/dataDisplay/button text — a distinct technical voice for UI chrome), and Pirata One (`--font-brand`, self-hosted at `app/public/fonts/PirataOne-Regular.woff2`, brand moments only). The three Google-hosted families load via `<link>` tags in `app/index.html`, not a CSS `@import` — Vite/Tailwind's production CSS build (Lightning CSS) silently drops remote `@import` statements, which was quietly breaking all three fonts on the deployed site until this was caught and fixed. Instrument Sans and Chivo Mono replaced an earlier Inter-only setup after a typography-inspiration pass against rig.ai; Bebas Neue started as a one-off fix for the style-guide page's own headline and was promoted to the full heading system once it proved out; caption/dataDisplay were added and the whole kit's spacing normalized in a design-system finalization pass — palette and Pirata One stayed as they were throughout. Every interactive control (Button, TextInput, Modal's actions, `<Panel interactive>` when given an `onClick`) guarantees the 44px touch-target minimum and real keyboard operability, not just hover styling. The style-guide page's shell was rebuilt to match `styleguide-mockup.html` (repo root — the approved target composition): a masthead with mono doc-metadata and a Bebas title, numbered section eyebrows (`001 // FOUNDATION`), a sticky side index with real anchors, smooth scroll, and scroll-spy highlighting (hidden under 800px), hairline dividers under each section head, and every component state shown in a labeled specimen cell (`Section.tsx`/`Specimen.tsx`/`Index.tsx`/`Masthead.tsx`/`Footer.tsx` in `app/src/pages/style-guide/`, page-local — not part of the UI kit). All 15 sections that existed before that rebuild migrated in; Badge/StatusChip and TextInput/Modal merged into single "Badges & Chips" / "Inputs & Modals" sections per the mockup's grouping. The design-system audit-fix list is now closed except one small item: `BadgeTone` is still the shared six-tone type (imported into StatTile/TorchTimer/StatusChip from `Badge.tsx`) rather than living as its own named `Tone` type — works fine, just an odd home for it. `pnpm verify` (audit + typecheck + lint + test + build) is wired up locally and in `.github/workflows/verify.yml`. **Button labels are always uppercase**, baked into `Button.tsx` directly (not onto the shared `caption` level, which other callers like Badge and LogEntryRow need in their natural casing) — same "bake it into the specific usage, not the shared level" pattern as `display`'s wordmark casing. Fixed alongside, in two rounds: `JournalComposer`'s kind-toggle pills first had `min-h-11` (the touch-target minimum) but were missing the `inline-flex items-center justify-center` every other interactive pill/button in the kit uses to center content, which read as oversized/mis-proportioned rather than genuinely mis-padded (round 1). User feedback after seeing round 1 live was that the pills were still too big — the `min-h-11` forced 44px box itself was the actual problem, not just off-center content. Round 2 dropped `min-h-11` entirely and switched padding to Badge/StatusChip's own compact `px-3 py-1` (matching journal-mockup.html's `.kind` chip, `padding:5px 12px`), and added `uppercase` to the pill labels (per-component, same pattern as Button.tsx, not baked into the shared `caption` level). This is a deliberate, scoped exception to the standing "every interactive control gets the 44px touch-target minimum" rule for this specific dense chip-row pattern — not a change to the rule itself.
- **No `pnpm-lock.yaml` committed yet.** The environment that scaffolded `/app` had `registry.npmjs.org` blocked by its network egress settings, so `pnpm install` couldn't be run there and no real lockfile could be generated — every dependency version in `app/package.json` is a hand-picked range, not something `pnpm install` has actually resolved and verified yet. CI currently runs `pnpm install --no-frozen-lockfile` as a stopgap. **As soon as `pnpm install` succeeds anywhere** (your machine, most likely), commit the resulting `app/pnpm-lock.yaml` and switch `.github/workflows/verify.yml` back to `pnpm install --frozen-lockfile` — this is a known gap to close, not a decision to leave in place.
- Repo `Yetiouz/grimoire`, linked to two Vercel deploy targets: the landing page auto-deploys to `grimoire-sable.vercel.app`, and `/app` auto-deploys separately to `grimoire-app-ten.vercel.app`. Both confirmed live and in sync with `main` (checked 2026-08-05 against that day's push: the app target is serving the rebuilt style-guide shell, the Portraits rename, and the new EmptyState/Skeleton/ErrorBanner specimens). This doc previously said `/app` wasn't wired to a deploy target yet — that was stale; it already was. **`app/vercel.json` added**: the `grimoire-app` Vercel project's Root Directory is `app` (confirmed in its Build and Deployment settings), and with no SPA-fallback rewrite configured, Vercel returned a bare 404 for any path other than `/` — including `/style-guide`, since Vite/React never got a chance to read the URL and render it. `app/vercel.json` adds a catch-all rewrite (`"/(.*)"  →  "/index.html"`) so every path serves the app shell and lets `App.tsx`'s own pathname check take over; built static assets (JS/CSS/fonts under the output directory) are still served directly since Vercel's `rewrites` only apply after the filesystem check fails.
- **Supabase project provisioned**: `grimoire` (ref `zvbpnxjpbecyahylhaar`, region us-east-2, `https://zvbpnxjpbecyahylhaar.supabase.co`, free tier). Fresh, not the old attempt-1 project (`ylobsexyfdumiweoltls`, "Yetiouz's Project") — that one's database was empty but not actually clean (a leftover active Edge Function, `ai-gm-turn` v15, was still deployed on it), so a real fresh start was worth the few minutes over trusting an unverifiable "wiped" claim. The old project is left alone, untouched, no cost. The code side is wired: `@supabase/supabase-js` is a dependency, `app/src/lib/supabase.ts` exports the one client (reads `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`, throws a clear error immediately if either is missing rather than failing opaquely later), `app/.env.example` documents both vars (neither is a secret — RLS gates access, not the anon key), and `app/.gitignore` excludes `.env*.local`. **Done**: GitHub OAuth App registered and enabled in Supabase's Authentication → Providers → GitHub; `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` added to the `grimoire-app` Vercel project's env vars (Production + Preview) and deployed live at `grimoire-app-ten.vercel.app`. **Still open**: enabling email-magic-link as the documented auth fallback. Nothing else is blocking auth/data work — first thing Milestone 1 needs.
- **Journal v1 built (Part A + Part B), the today-value first slice.** Part A (data model — five tables, member-scoped RLS, four `SECURITY DEFINER` commands, two advisor-fix migrations) is `supabase/migrations/0001`–`0003`; see the SPEC decisions log for its full verification record. Part B (frontend) landed on top of it: the app is no longer just the style guide — `App.tsx` now gates on `useAuth` (`app/src/hooks/useAuth.ts`, GitHub-OAuth-only per SPEC) and drives a plain-state, router-free flow across three screens: sign-in (`app/src/pages/auth/SignIn.tsx`), campaign list (`app/src/pages/campaigns/CampaignList.tsx` — Panel cards, a Modal with a name-only field, all four SPEC screen states), and the journal (`app/src/pages/journal/JournalScreen.tsx` — campaign header, "Start session," all four states). A new `components/domain/` tier sits between `components/ui/` and the pages: `JournalFeed` (entries + session dividers + optional composer, filter-driven, renders identically wherever it's placed per SPEC's component-boundary rule) and `JournalComposer` (kind chips + text field + Log button, gated on an open session). `app/src/lib/campaigns.ts` is the data-access layer wrapping the four RPC commands plus reads. `LogEntryRow` grew from 3 kinds to the full 5-kind taxonomy (`narration`/`action`/`roll`/`note`/`system`) as part of this pass — audit-fix along the way: `system`'s old panel-card background actually belonged to `narration` per the approved mockup; `system` itself just gets reduced opacity and smaller/fainter text. One flagged deviation from the approved mockup at the time: the composer rendered inline at the bottom of the feed, not as a fixed/sticky bottom bar — since resolved, see the shell-polish bullet below. One flagged gap: entry `actor_color` for the signed-in player's own entries uses a hardcoded `PLAYER_COLOR` placeholder constant (`#9b5cff`, matching the app's one accent and the mockup's own Bjorn example) — SPEC's "one PC color everywhere" rule assumes a character model that's out of scope for this slice; replace this constant once one exists. `App.test.tsx` was also fixed in passing: its old assertion checked for a heading literally named "Style guide," which no longer matches anything on the page (the masthead's real h1 is "Design System") and had already been failing before this change — unrelated drift from the style-guide shell rebuild, caught while this file needed touching anyway. **Bug caught post-deploy and fixed**: none of the three new screens ever set a background color — only `StyleGuide.tsx` happened to wrap itself in `bg-bg text-ink` on its own root, which masked that `body` itself has never had the dark theme applied. Every new Journal v1 screen rendered on the browser's default white background with near-white `text-ink` on top of it (functionally invisible). Fixed globally in `index.css` (`body { background-color: var(--color-bg); color: var(--color-ink); min-height: 100vh; }`) rather than patching each page — per-page `bg-bg`/`text-ink` like `StyleGuide.tsx` still has is now redundant but harmless.
- **Journal v1 shell-polish pass** (post-review, all composition — no data/component API changes): three fixes plus one confirmation, requested after the user tried the deployed slice. (1) **Composer pinned to the viewport bottom**, per `journal-mockup.html`'s `.composer`: wrapped at the `JournalScreen` call site (not inside `JournalComposer` itself, which stays a plain content block any future host can lay out differently) in a `fixed inset-x-0 bottom-0` bar with a blurred/translucent background and top border. This needed two hand-written CSS rules in `index.css` (`.composer-clearance`, `.composer-safe-bottom`) rather than Tailwind utilities — the closed 4–64px spacing scale is for design-system spacing decisions, not fixed-element clearance or `env(safe-area-inset-bottom)` device-inset math, so these are a deliberate, documented exception to "spacing only from the closed scale," the same class of exception as the font-face/body rules already in that file. `JournalFeed` also grew an internal `useRef`/`useEffect` (no prop change) that scrolls to a bottom sentinel whenever the visible entry count changes, including on mount, so the newest entry always lands just above the fixed bar. (2) + (3) **Shared `PageHeader`** (`app/src/components/ui/PageHeader.tsx`, new kit component) gives Campaigns and Journal the same header-band treatment as the style guide's own `Masthead.tsx` — a `text.label` meta strip above a Bebas `h1`, inside a bottom-hairline band — scaled to the app screens' `max-w-2xl` body instead of the style guide's wider one. Campaigns now shows "Grimoire" / Sign out in that strip (Sign out moved out of `App.tsx`'s `AuthGate`, where it previously floated alone above the list with no header at all) with "Campaigns" as the title; `CampaignList.tsx` no longer renders its own `<h1>`, just a right-aligned "New Campaign" action row. Journal shows the "← Campaigns" breadcrumb / a `Solo · Session N` meta line (the session number is real, sourced from `sessions` — prefers the open session, falls back to the most recent one, says "No sessions yet" if there are none; "Solo" itself is a static placeholder since game-mode isn't a modeled field yet) with the campaign name as the title. **Confirmed**, not changed: `rounded-button` (11px) is the only radius Button ever applies (`Button.tsx` line 41) — no accidental `rounded-full` on any Button call site; the pill-shaped controls are `Badge`/`StatusChip`/the composer's kind chips specifically, which use `rounded-full` deliberately as tags, not buttons.
- **Journal header session control** (immediate follow-up to the shell-polish pass above): the standalone "Start session" / "Start next session" button that sat as its own row below the header moved into the header band itself, via a new `PageHeader` slot — `titleAction`, rendered beside the `h1` rather than in the meta strip above it, for a primary action that belongs with the heading. It's one `Button`, two states, same `handleStartSession` click handler throughout (still Amendment 2: no separate "end session" command — starting the next session is how one ends). No open session: reads "Start Session." An open session: relabels to "In Session" with a live dot (`h-2 w-2 rounded-full bg-green`, StatusChip's existing tone-dot vocabulary, no new color) — but **stays clickable**, by explicit user decision (asked directly rather than assumed, since the alternative — a passive, non-clickable status pill with the session-ending action moved somewhere else — was also on the table): clicking it while live still starts the next session. A native `title` tooltip on the button spells that out, since the relabel alone could otherwise read as a passive indicator. Also asked and confirmed: session status stays two states (live / not started) rather than adding a real "paused" state to the data model — `sessions` only ever tracks `ended_at` null-or-set today, and a third state would be a real schema change, out of scope for a header tweak.
- **The Black Road import** (BUILD_PLAN.md slice 2) built and applied live: `supabase/migrations/0004_black_road_import.sql` — five new tables (`characters`, `npcs`, `factions`, `quests`, `treasure`), same member-scoped-SELECT-only RLS shape as 0001, plus `'chat'` added to `journal_entries`' kind check constraint (the vision-handoff decision's flagged follow-up). Full detail — the three scoping decisions (fine-grained journal decomposition, character-sheets-over-tracker.xlsx, full import), the schema choices (jsonb character stats, hirelings folded into `npcs`, free-text quest status/treasure quantity), the exact row counts (144 journal entries, 3 characters, 17 npcs, 2 factions, 7 quests, 13 treasure items), and the dev-test-data cleanup this migration also performed — is recorded in SPEC.md's decisions log (two entries, both dated 2026-08-05: scoping, then the build). Applied directly to the live `grimoire` Supabase project via five sequential migration calls (tool payload limits, not a design choice); `get_advisors` re-run clean afterward. Not yet built: any UI or command surface reading this data — PlayerCard/CharacterSheet (next queued slice), quest log panel, NPC/faction/treasure trackers (slices 5/6) all still need their own vertical build-out. `Constantine` and `LaLa` imported with `status: 'awaiting'` (not currently in play, matching campaign-state.md's own "Awaiting family campaign" framing) — only Kimbo Slice is `status: 'active'`.
- **Character colors** (`supabase/migrations/0005_character_colors.sql`), a small gap-closer at the start of BUILD_PLAN.md slice 3: adds `characters.color text`, backfilled with the existing theme hex values (Kimbo Slice `#9b5cff`, Constantine `#35f0ff`, LaLa `#39ff8f`). Closes the flagged `PLAYER_COLOR` placeholder gap noted in the Journal v1 Part B bullet above, now that a real character model exists. `database.types.ts` regenerated.
- **BUILD_PLAN.md slice 3 built**: PlayerCard + CharacterSheet, specced directly from `player-view-mockup.html` per the "from the latest mockup" resolution (awaiting PCs dim per `.pcard.offline`, don't hide; a new `Overlay` primitive rather than reusing `Modal`, since the Sheet's content is much larger than Modal's confirm-dialog shape). New: `components/ui/Overlay.tsx` (full-screen backdrop + scrollable panel, Escape/backdrop-click close — Maps/Dice will reuse it later); `components/domain/TalentRow.tsx`, `SpellCard.tsx`, `GearSlotGrid.tsx`, `PlayerCard.tsx`, `CharacterSheet.tsx`; `lib/characters.ts` (`listCharacters()` + safe jsonb readers for `gold`/`abilities`/`sheet`, mirroring `campaigns.ts`'s pattern); one new closed-set icon, `bless`. `JournalScreen.tsx` now loads characters, renders the party row above the feed, opens `CharacterSheet` on card click, and replaces the old hardcoded `PLAYER_COLOR` with the real color read from the active character. Every field not present in the real imported data (talent uses-remaining, spell tier/lock state, numbered gear slots, torch/luck icons) is left out rather than fabricated — see SPEC.md's decisions log for the full account, including this pass's verification method (`esbuild` syntax + bundle-resolution checks, since this sandbox has no npm registry access and neither it nor the user's mounted repo folder has `node_modules` installed — a real `tsc`/build pass still needs to run on the user's own machine).
- **`end_session` command added** (`supabase/migrations/0006_end_session.sql`), reversing part of Journal v1's Amendment 2 (there was no separate end-session command; starting the next session was the only way to end one — a deliberate decision at the time, revisited after real use showed it was a real gap: no way to just stop for the night without opening a new, empty next session). Confirmed directly before building: a plain end (closes the open session, no new one), not a resumable pause — no schema change beyond the command itself. Same `SECURITY DEFINER`/membership-check shape as every other command; both the `public` and `anon` execute revokes done in this migration from the start, not learned the hard way like 0002/0003 had to. `JournalScreen`'s header control is now two real actions ("Start Session" / a live indicator plus "End Session") instead of one button doing double duty. `lib/campaigns.ts` gained `endSession()`; `database.types.ts` regenerated.
- **BUILD_PLAN.md slice 4 built: server-authoritative dice.** New `roll_dice` command (`supabase/migrations/0007_roll_dice.sql`) — a pure RNG primitive using Postgres's `random()` server-side, returns jsonb, no journal write of its own and no schema change beyond the function itself; same `SECURITY DEFINER`/membership-check shape, both execute revokes done from the start this time. `lib/dice.ts` wraps it plus `formatRollText()` (the confirmed "10 + 2 (STR Modifier) = 12" convention). New `components/domain/DiceRoller.tsx`: die/count/mode/modifier picker feeding a Roll → preview → Log flow, modifiers sourced from the character's real ability scores (plus a Custom free-number option), reached via a new dice-trigger button on `JournalComposer` (`onOpenDice` prop, gated like Log). The existing hand-rolled "Roll" kind chip on `JournalComposer` is deliberately untouched — confirmed directly with the user that typed/hand-rolled entry must stay available alongside the new app-side roller. Full reasoning (crit/fumble scope call, why `DiceResult` wasn't reused, the modifier-sourcing decision) is in SPEC.md's decisions log.

## Decided (do not relitigate)

- TypeScript, React 18, Vite, Tailwind, Supabase, Vercel, pnpm. Single frontend, single backend.
- Design system before screens; screens compose from the UI kit. Nothing ships visually plain.
- Archived attempt-1 code is reference-only — read for lessons/shapes, never paste from it.
- No component file past ~300 lines; split instead.
- Every meaningful game-state mutation goes through an authoritative command and the append-only event ledger. The AI GM uses the same commands as a human GM.
- Every schema change is a numbered migration; rebuild + authorization tests before applying.

## Workflow rules

- Read SPEC.md before implementing anything. If a feature isn't specced, say so and ask — don't invent requirements.
- For any change touching more than one file: present a plan first, wait for approval, then implement. Full workflow is in WORKFLOW.md.
- Every task ends with evidence: test output, a build result, or a screenshot of the deployed page. Never claim done without showing the check.
- Keep the landing page (`index.html`) and the app separate. Landing-page edits must not touch app code and vice versa.
- Commit after every working slice with a descriptive message. Small commits — they double as deploys and rewind points.

## Style

- Match the landing page's design language: dark theme, CSS variables in `:root` (see `index.html` — `--bg`, `--panel`, `--purple`, etc.). In the app: Instrument Sans for body/UI text, Bebas Neue for h1/h2/h3 headlines (condensed, uppercase), Chivo Mono for labels/numerics/captions/dataDisplay/buttons, Pirata One for brand headers — see the style guide's Typography section for the full closed set. Spacing is a closed scale too (4/8/12/16/24/32/48/64px, six named slots) — see the Spacing section right after it.
- Shadowdark game content is third-party licensed — flag before reproducing rules text verbatim; original mechanics references are fine.
